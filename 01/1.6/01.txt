Подвиг 1. В какой момент вызывается магический метод __new__()?

непосредственно перед созданием объекта класса      +

непосредственно перед уничтожением объекта класса сборщиком мусора

после вызова инициализатора класса - метода __init__()

сразу после создания объекта класса